local bufferline = require 'bufferline'

bufferline.setup {
  options = {
    -- indicator = {
    --   style = 'underline',
    -- },
    separator_style = 'slant',
    show_buffer_icons = false,
  },
}
